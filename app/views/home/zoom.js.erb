var image = $(".wrapper img");
image.hide();
image.attr("src", "<%= j @project.main.url %>").load(function() {
  image.show();
});
$(".wrapper .inside-project-pager").replaceWith("<%= j render "shared/project_pager" %>");
